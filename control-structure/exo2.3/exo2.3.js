let i = 1; // Initialise une variable i à 1

//pour la boucle while

while (i <= 100) { // Tant que i est inférieur ou égal à 100...
  if (i % 2 === 0) { // Si i est pair (le reste de la division par 2 est égal à 0)...
    console.log(i); // Affiche i (le nombre pair)
  }
  i++; // Incrémente i pour passer au prochain nombre
}


//pour la boucle for

for (let i = 1; i <= 50; i++) { // Initialise i à 1 ; Tant que i est inférieur ou égal à 100 ; Incrémente i
    if (i % 2 === 0) { // Si i est pair (le reste de la division par 2 est égal à 0)...
      console.log(i); // Affiche i (le nombre pair)
    }
  }